{% extends "admin/dashboard.html.twig" %}

{% block content %}

    <form action="{{ path("admin_comment_edit", {id: comment.id}) }}" method="post">

        <div class="form-group">
            <a href="{{ path("admin_event", {id: comment.event.id}) }}" target="_blank" class="btn btn-dark">Event</a>
        </div>

        <div class="form-group">
            <a href="{{ path("admin_customer", {id: comment.user.id}) }}" target="_blank"  class="btn btn-dark">Customer</a>
        </div>

        <div class="form-group">
            <label for="comment">Comment:</label>
            <textarea class="form-control"  id="comment" name="comment" rows="5">{{ comment.comment }}</textarea>
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>

{% endblock %}
